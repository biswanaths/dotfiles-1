## Aliases ##

# General aliases
alias c="clear"
alias cp="cp -vi"
alias mv="mv -vi"
alias rm="rm -v"
alias curl="/usr/local/Cellar/curl/7.37.0/bin/curl"
alias irc="weechat-curses"

# Fasd Aliases
alias j="z"
alias v="f -e vim"

# ls aliases
alias ls="gls --color -A"
alias ll="gls --color -A -l"        # List format
alias le="gls --color -d *(-/DN^F)" # List empty directories

# Git aliases
alias g="git"
alias gcopy="git log -n 1 | head -n 1 | cut -d ' ' -f2 | tr -d '\n' | pbcopy && echo 'Copied \c' && pbpaste && echo ''"
alias gst="git status -sb"

# Edit stuff
alias e=$EDITOR
alias tconf="vim ~/dotfiles/.tmux.conf"
alias vconf="vim ~/dotfiles/.vimrc"
alias zconf="vim ~/dotfiles/.zshrc"

# Vimutil aliases
alias vbund=". ~/dotfiles/.vimutil/upgrade_bundles"
alias vins=". ~/dotfiles/.vimutil/install_bundles"
alias vupd=". ~/dotfiles/.vimutil/upgrade_vim"
alias ebund="vim ~/dotfiles/.vimutil/upgrade_bundles"
alias eins="vim ~/dotfiles/.vimutil/install_bundles"
alias eupd="vim ~/dotfiles/.vimutil/upgrade_vim"

# Useful aliases
alias atom="open -a atom"
alias betty="~/Documents/Repositories/betty/main.rb"
alias bust="buster generate --domain=http://127.0.0.1:2368 && buster deploy"
alias compl="rm -rf ~/.zcomp* ~/.cache/zcomp* && compinit"
alias flush="dscacheutil -flushcache"
alias history-stat="history 0 | awk '{print \$2}' | sort | uniq -c | sort -n -r | head"
alias muxd="tmuxinator start default"
alias pgrep="pgrep -l"
alias ping="ping -c 5"
alias binstubs="bundle install --binstubs .bundle/bin && rbenv rehash"
alias tat="tmux att"
alias swi="xcrun swift -i"
alias unzip="unzip -d ~/Downloads"
alias upd="brew update; brew outdated; brew upgrade"
# vim: ft=zsh
